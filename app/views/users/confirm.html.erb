<div class="container">
  <div class="row">
    <div class="confirm-container">
      <h3 class="title">退会確認画面</h3>
      <p>本当に退会しますか？</p>
      <p>退会するとログインできなくなります。</p>
      <%= link_to '退会しない', user_path(current_user), class: 'btn btn-default' %>
      <%= link_to '退会する', withdrawal_path(current_user), method: :patch, class: 'btn btn-default' %>
    </div>
  </div>
</div>